version: '3'
services:
  odometer:
    build: .
    container_name: SimpleMultiTimer
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    volumes:
      # This mapping ensures your database survives if you delete the container
      - ./data:/app/data